version: 2
jobs:
  build:
    docker:
      - image: debian:stretch

    steps:
      - checkout

      - run:
          name: install dependencies
          command: apt-get install build-essential

      - run:
          name: build all
          command: make all

      - run:
          name: build test
          command: make test
